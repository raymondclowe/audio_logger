[project]
name = "audio-logger"
version = "0.1.0"
description = "Continuous audio recording and transcription logger"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "numpy>=1.24.0",
    "psutil>=5.9.0",
    "jiwer>=3.0.4",
    "optuna>=4.0.0",
]
